---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: wukongsnapshots.vm.novasphere.dev
spec:
  group: vm.novasphere.dev
  names:
    kind: WukongSnapshot
    listKind: WukongSnapshotList
    plural: wukongsnapshots
    singular: wukongsnapshot
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WukongSnapshot is the Schema for the wukongsnapshots API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WukongSnapshotSpec defines the desired state of WukongSnapshot
            properties:
              wukongName:
                description: WukongName is the name of the Wukong instance to snapshot
                type: string
            required:
            - wukongName
            type: object
          status:
            description: WukongSnapshotStatus defines the observed state of WukongSnapshot
            properties:
              creationTime:
                description: CreationTime is the time when the snapshot was created
                format: date-time
                type: string
              error:
                description: Error message if the snapshot failed
                type: string
              phase:
                description: |-
                  Phase represents the current phase of the snapshot
                  Valid values: Pending, Creating, Succeeded, Failed
                type: string
              snapshotName:
                description: SnapshotName is the name of the underlying KubeVirt VirtualMachineSnapshot
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
